# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
package.json
package-lock.json
site/node_modules/
site/package-lock.json

# Build (ignore the generated site, but keep the committed Featured Trend feed
# snapshot so the repo stays in sync with the latest trend each day)
site/docs/*
!site/docs/featured-trend.json

# Env / IDE
.env
.vscode/
.idea/
uv.lock
