---
title: "AutoBNN: Probabilistic time series forecasting with compositional bayesian neural networks"
date: 2025-11-20
type: news
rank: 3
source: "Google AI Blog"
source_url: "http://blog.research.google/2024/03/autobnn-probabilistic-time-series.html"
published: 2024-03-28T13:53:00-07:00
layout: "layout.njk"
tags:
  - news
  - issue
---

## What Happened

AutoBNN is a new model for probabilistic time series forecasting that uses compositional Bayesian neural networks (CBNNs) to generate probabilistic forecasts. This is a significant advancement in the field of machine learning and has the potential to revolutionize the way we forecast time series data.

The model has several key advantages, including:

* **Generative nature**: CBNNs can generate new time series samples that are similar to the training data. This allows the model to learn complex patterns in the data and to make accurate forecasts even for data points that are not present in the training data.
* **Probabilistic nature**: CBNNs can output probability distributions for the future values of the time series. This allows us to make more accurate forecasts and to quantify the uncertainty associated with the forecast.
* **Data-driven**: CBNNs are trained on historical data, which allows them to learn from the data and to make accurate forecasts even for data points that are not present in the training data.

The model has been tested on a variety of real-world time series datasets, and it has consistently performed well. This suggests that it has the potential to be a powerful tool for improving forecasting accuracy.

## Why It Matters

AutoBNN is a major breakthrough in the field of machine learning, and it has the potential to revolutionize the way we forecast time series data. This is because CBNNs can generate new time series samples that are similar to the training data. This allows the model to learn complex patterns in the data and to make accurate forecasts even for data points that are not present in the training data.

Furthermore, CBNNs can output probability distributions for the future values of the time series. This allows us to make more accurate forecasts and to quantify the uncertainty associated with the forecast.

The model has the potential to make a significant impact on a wide range of industries. For example, CBNNs could be used to improve weather forecasting, financial market predictions, and supply chain management.

## Context & Background

CBNNs are a type of artificial neural network that is particularly well-suited for time series forecasting. CBNNs are based on the idea of representing time series data as a composition of simpler, underlying components. The model then learns to predict the future values of the time series by combining these underlying components.

CBNNs have been shown to be very effective for time series forecasting. This is because CBNNs are able to learn complex patterns in the data and to make accurate forecasts even for data points that are not present in the training data.

## What to Watch Next

Researchers are currently working on improving the performance of CBNNs. They are also exploring new ways to use CBNNs for time series forecasting, such as using CBNNs to generate synthetic data.

---

**Source**: [Google AI Blog](http://blog.research.google/2024/03/autobnn-probabilistic-time-series.html) | Published: 2024-03-28